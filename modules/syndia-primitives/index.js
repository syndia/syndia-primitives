const colors = require(`@syndia/ds-colors`)
const space = require(`@syndia/ds-space`)
const { fonts, fontSize, lineHeights } = require(`@syndia/ds-typography`)

const theme = {
  colors,
  fonts,
  fontSizes,
  lineHeights,
  space,
}

module.exports = theme